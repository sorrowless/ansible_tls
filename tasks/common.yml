---
- name: TLS | Ensure directories
  become: true
  become_user: root
  ansible.builtin.file:
    path: "{{ tls.target_dir }}"
    state: directory
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: 0755
